# Supabase 공동 편집 설정

## 1. 데이터베이스 만들기

1. [Supabase Dashboard](https://supabase.com/dashboard/project/bilrspsenkemhbzqnrlp/sql/new)의 **SQL Editor**를 엽니다.
2. 프로젝트의 `supabase-setup.sql` 내용을 전부 붙여 넣습니다.
3. **Run**을 눌러 실행합니다. 이메일 로그인 버전을 이미 설치한 경우에도 전체를 다시 실행하면 공유 코드 방식으로 전환됩니다.

공유 코드 원문은 DB에 저장하지 않고 bcrypt 해시만 저장합니다. 테이블 직접 접근은 차단되며 코드 검증 RPC를 통해서만 체크 상태를 읽고 수정할 수 있습니다.

## 2. 사용하기

1. GitHub Pages의 `gpt-v2.html`을 엽니다.
2. 처음 표시되는 공동 편집 창에 두 사람이 정한 공유 코드를 입력합니다.
3. 연결되면 상단 상태가 **동기화 ✓**로 바뀝니다.

기존 브라우저의 추천 선택과 체크 상태는 첫 연결 때 Supabase에 병합됩니다. 이후 두 사람의 상태는 약 5초 간격으로 반영됩니다. 인터넷이 잠시 끊기면 기기에 먼저 저장하고 연결이 돌아왔을 때 재전송합니다.

## 3. 뷰포인트 참고 사진 (2026-08 추가)

추천·팁 탭의 **뷰포인트** 카드마다 참고 사진을 올릴 수 있습니다. 웹에서 마음에 드는 사진을 폰에 저장한 뒤 카드의 **＋ 참고 사진 올리기**를 누르면 두 사람 모두에게 보입니다.

**이 기능을 쓰려면 `supabase-setup.sql`을 다시 한 번 실행해야 합니다.** 기존 내용은 `create or replace`라 다시 돌려도 안전하고, 뒤쪽에 `trip_photos` 테이블과 사진용 RPC 4개가 추가되어 있습니다.

동작 방식:

- 올린 사진은 캔버스로 **긴 변 1200px·JPEG 품질 0.72**까지 줄여 **300KB 안**으로 압축합니다. 폰 사진 원본(3~5MB)을 그대로 올리지 않습니다.
- 먼저 브라우저의 **IndexedDB**에 저장해 즉시 표시하고, 그다음 Supabase로 전송합니다. localStorage(약 5MB)를 쓰지 않으므로 체크·지출·메모 데이터와 용량을 다투지 않습니다.
- 연결이 끊긴 상태에서 올리면 카드에 *이 기기에만 저장됨*으로 표시되고, 연결이 돌아오면 자동으로 전송됩니다.
- 5초 폴링에는 **목록만**(`get_trip_photo_index`) 실립니다. 사진 본문은 바뀐 것만 한 장씩 내려받으므로 통신량이 늘지 않습니다.
- 상대가 사진을 지우면 내 기기에서도 함께 정리됩니다.
- SQL을 아직 실행하지 않았어도 앱은 오류 없이 동작합니다. 사진이 이 기기에만 저장될 뿐입니다.

RPC 상한은 700KB이고 `data:image/` 로 시작하지 않는 값은 거부합니다. 실수로 원본이 올라가는 것을 막는 장치입니다.

⚠️ 올린 사진은 Supabase에만 저장되고 **Git 저장소에는 들어가지 않습니다.** 공개 저장소에 남의 사진이 재배포되지 않으므로 웹에서 찾은 참고 사진을 개인적으로 보관하기에 적합합니다.

## 보안

- HTML에 포함된 Publishable key는 브라우저 공개용 키입니다.
- Secret key와 `service_role` key는 HTML이나 Git 저장소에 넣으면 안 됩니다.
- 공유 코드 원문은 HTML과 Git 저장소에 포함되지 않습니다.
- 링크와 공유 코드를 모두 아는 사람은 데이터를 수정할 수 있으므로 코드를 외부에 공개하지 마세요.
